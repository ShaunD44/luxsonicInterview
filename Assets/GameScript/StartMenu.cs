using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class StartMenu : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
// This should be writen so when start is selected the items spawn and you can start picking them up and placing them where they are needed once touching the collider and you let go you cant pick up until it restarts
// Then the collider should detect the correct item it has colided with play
// a sound and log one correct choice. Once the code tracks 4 correct choices a win screen comes up. 